<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>CyberGuard AI Security Incident Report</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      margin: 0;
      padding: 0;
      color: #333;
    }
    .container {
      max-width: 800px;
      margin: 0 auto;
      border: 1px solid #ddd;
      border-radius: 5px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }
    .header {
      background-color: #1a237e;
      color: white;
      padding: 20px;
      border-top-left-radius: 5px;
      border-top-right-radius: 5px;
    }
    .content {
      padding: 20px;
    }
    .footer {
      background-color: #f5f5f5;
      padding: 15px 20px;
      font-size: 12px;
      color: #666;
      border-bottom-left-radius: 5px;
      border-bottom-right-radius: 5px;
    }
    .threat-badge {
      display: inline-block;
      background-color: <%= 
        threatLevel === 'HIGH' ? '#e74c3c' : 
        threatLevel === 'MEDIUM' ? '#f39c12' : 
        threatLevel === 'LOW' ? '#3498db' : '#95a5a6' 
      %>;
      color: white;
      padding: 5px 10px;
      border-radius: 3px;
      font-weight: bold;
    }
    table {
      width: 100%;
      border-collapse: collapse;
      margin: 15px 0;
    }
    table, th, td {
      border: 1px solid #ddd;
    }
    th, td {
      padding: 10px;
      text-align: left;
    }
    th {
      background-color: #f2f2f2;
    }
    .logo {
      width: 60px;
      height: 60px;
      margin-right: 15px;
      vertical-align: middle;
    }
    .timestamp {
      color: #777;
      font-style: italic;
      margin-top: 15px;
    }
    .recommendations {
      background-color: #e8f5e9;
      padding: 15px;
      border-radius: 5px;
      margin: 15px 0;
    }
    .alert-badge {
      text-align: center;
      margin: 20px 0;
    }
    .alert-icon {
      font-size: 48px;
      color: <%= 
        threatLevel === 'HIGH' ? '#e74c3c' : 
        threatLevel === 'MEDIUM' ? '#f39c12' : 
        threatLevel === 'LOW' ? '#3498db' : '#95a5a6' 
      %>;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>CyberGuard AI Security Alert</h1>
      <h2><%= title %></h2>
    </div>
    
    <div class="content">
      <div class="alert-badge">
        <div class="alert-icon">⚠️</div>
        <h3>Threat Level: <span class="threat-badge"><%= threatLevel %></span></h3>
      </div>
      
      <p><strong>Incident Summary:</strong></p>
      <p><%= description %></p>
      
      <% if (details) { %>
        <h3>Incident Details</h3>
        <table>
          <tbody>
            <% Object.entries(details).forEach(([key, value]) => { %>
              <tr>
                <th><%= key %></th>
                <td><%= value %></td>
              </tr>
            <% }); %>
          </tbody>
        </table>
      <% } %>
      
      <% if (recommendations) { %>
        <div class="recommendations">
          <h3>Security Recommendations</h3>
          <% if (Array.isArray(recommendations)) { %>
            <ul>
              <% recommendations.forEach(recommendation => { %>
                <li><%= recommendation %></li>
              <% }); %>
            </ul>
          <% } else { %>
            <p><%= recommendations %></p>
          <% } %>
        </div>
      <% } %>
      
      <p class="timestamp">
        Incident detected on: <%= new Date(date).toLocaleString() %><br>
        Report generated by: <%= generatedBy %> (v<%= version %>)
      </p>
    </div>
    
    <div class="footer">
      <p>This is an automated security report from CyberGuard AI. If you have any questions or need assistance, please contact your security administrator.</p>
      <p>© <%= new Date().getFullYear() %> CyberGuard AI. All rights reserved.</p>
    </div>
  </div>
</body>
</html>
